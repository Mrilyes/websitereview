import "./globals.css";

export const metadata = {
  title: "Testing Review",
  description: "Visual review proxy rebuilt with Next.js 16",
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
